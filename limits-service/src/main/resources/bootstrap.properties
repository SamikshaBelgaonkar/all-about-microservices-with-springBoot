spring.application.name=limits-service
#it will fetch values from spring-cloud-config-server and not from limits-service(application.properties) 
spring.cloud.config.uri=http://localhost:8888

 #it will take values from spring cloud config dev env
spring.profiles.active=dev 

